apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - service.yaml
  - deployment.yaml
  # Optional: For gated HuggingFace models, create the secret manually:
  #   oc create secret generic hf-token --from-literal=token=hf_xxx -n vllm-omni
  # Or uncomment secret.yaml and add your token, then uncomment the line below:
  # - secret.yaml
