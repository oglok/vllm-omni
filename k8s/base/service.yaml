apiVersion: v1
kind: Service
metadata:
  name: vllm-omni
  namespace: vllm-omni
  labels:
    app.kubernetes.io/name: vllm-omni
    app.kubernetes.io/component: inference
spec:
  type: ClusterIP
  selector:
    app: vllm-omni
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
